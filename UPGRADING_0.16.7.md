# 0.16.7 本地版升级与恢复说明

此版本保留 VS Code 原生合并编辑器 Result 区域中的 **标记为已解决** 文字按钮，同时修复 B01–B13。安装文件为 `overleaf-workshop-0.16.7-native-button.vsix`。

## 升级与启用

1. 保存正在编辑的文件和合并草稿，结束所有仍在使用旧版本同步该副本的 VS Code 窗口。旧版本没有新增的系统文件锁，不能与新版同时同步同一副本。
2. 通过 **Extensions: Install from VSIX…** 安装本地包。
3. 按 [原生按钮启用说明](NATIVE_MERGE_BUTTON.md) 为 `lipf1024.overleaf-workshop` 启用 `contribEditorContentMenu`，然后完全退出并重新启动 VS Code。仅安装 VSIX 或 Reload Window 不会启用该实验接口。
4. 打开本地副本并检查 Source Control。接管窗口会先检查项目身份与恢复日志，再开始同步。首次关联、损坏元数据和无法验证的操作会进入待检查状态。

同一真实副本目录同时只有一个新版窗口拥有同步权限。其他窗口可以编辑普通文件、查看同步状态；同步和冲突解决操作需要在持锁窗口完成。持锁窗口正常退出后，或进程崩溃导致系统释放文件锁后，观察窗口会尝试接管。观察窗口编译前也会核对当前本地内容是否已同步到 Overleaf。

## 冲突、草稿和暂停项

- 内容冲突继续提醒、标红并暂停该文件的同步。原文件和合并输入保持只读，Result 可编辑。
- 保存或关闭合并编辑器只保存草稿。点击 Result 中的 **标记为已解决** 后，扩展会检查本地、Overleaf 版本和最终同步结果；全部通过后才清除冲突。
- 取消确认、远端再次变化、请求结果不明或同步失败都会保留冲突和草稿。
- Source Control 的 **Paused** 组显示路径阻塞或未保存的编辑器缓冲区等原因。这些项目没有可合并内容时不会提供空白合并入口。
- 调整忽略规则后，被忽略的文件不再参与扫描、监听、手动同步、重试或恢复；已有基线、冲突、草稿及恢复数据继续保留。

## 本地替换与恢复

状态文件继续使用 schema 1，日志升级为 schema 3。新版兼容读取旧日志；遇到未知的新格式时停止写入。

替换、删除或移动文件前，扩展先准备结果并写入日志，再把实际原文件移入同级的独占 `.overleaf-sync-<id>` 恢复目录，校验移走的内容，最后以禁止覆盖的方式安装结果。若期间有外部编辑、路径变化或目标被重新创建，双方版本都会保留。文件系统不支持必要的安装方式时，操作在移动原文件前停止。

出现 **Concurrent versions were preserved** 等恢复提示时：

1. 运行 **Local Replica: Review Recovery Files / 本地副本：检查恢复文件**。
2. 比较恢复目录中的原文件和当前本地文件；需要手动合并时，先把希望保留的内容整理到当前文件中，再重新打开检查操作。
3. 确认后点击 **保留当前文件并恢复同步**。扩展再次核对检查期间文件是否变化，备份各版本，然后按当前内容重新校验同步。取消会保留原有暂停状态。

恢复操作不会覆盖后来出现的文件。不要删除未完成日志引用的 `.overleaf/sync` 数据或 `.overleaf-sync-*` 目录，也不要让旧版本处理未完成的 schema 3 日志。

普通备份默认保留 30 天，最多 500 MiB；未完成恢复引用不受此限制。对象清理保护基线、冲突、最新草稿、日志、打开的合并会话和进行中的操作。元数据损坏而无法确认引用时，相关清理保持暂停。

## 连接、编译与远端移动

连接、加入项目和读取设置合计最多自动尝试 5 轮，认证错误直接停止。排除问题后运行 **Overleaf: Retry Connection / Overleaf：重新连接** 开始新一轮。停止或退出后不会继续排队重试。

编译检查保存结果和本地同步状态；诊断与 PDF 双向跳转绑定发起操作的项目。远端移动与改名分别得到确认后才更新缓存；中途失败会刷新服务器文件树并报告实际已知路径。覆盖远端文件前会保存目标内容，错误信息提供备份位置；覆盖整个远端目录会明确拒绝。

## 本轮验收范围

自动回归涵盖 B01–B13、真实临时文件、多进程争用与崩溃接管、日志各阶段中断、网络与编译生命周期。实际原生窗口验收仍未完成：隔离 VS Code 启动被自动审批服务以 404 拒绝。因此本包不能作为“页面按钮已完成可见性和点击验收”的证明；最低声明版本 1.101.0 与当前安装版本 1.129.0 的窗口验收仍待完成。
